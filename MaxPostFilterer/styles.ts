import { CSSObject } from "@emotion/styled";

export const containerCSS: CSSObject = {
        display: 'flex',
        alignItems: 'center',
        flexDirection: 'column',
        marginBottom: '4rem',
        marginTop: '2rem'
};